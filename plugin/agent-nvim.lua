-- When using a plugin manager like lazy.nvim, setup() is called via the
-- config function. This file is only a fallback for manual runtimepath installs.
if not vim.g.agent_nvim_loaded then
  vim.g.agent_nvim_loaded = true
end
